version: '3.8'

services:
  backend:
    build: .
    # Removed host ports to avoid conflicts; Nginx handles public access
    volumes:
      - ./uploads:/app/uploads
      - ./database.db:/app/database.db
    env_file:
      - .env
    restart: always

  nginx:
    image: nginx:alpine
    ports:
      - "8081:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
    restart: always
